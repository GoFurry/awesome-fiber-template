package main

import (
	"fmt"
	"os"

	"{{module_path}}/cmd"
	projectversion "{{module_path}}/internal/version"
)

func main() {
	_, _, _ = projectversion.Version, projectversion.Commit, projectversion.BuildTime
	if err := cmd.Execute(os.Args[1:]); err != nil {
		fmt.Fprintf(os.Stderr, "error: %v\n", err)
		os.Exit(1)
	}
}
