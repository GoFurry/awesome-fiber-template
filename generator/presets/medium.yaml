name: medium
summary: balanced HTTP service baseline
description: Keeps a production-oriented HTTP service setup while trimming scheduler and Prometheus complexity.
base: medium
default_capabilities: []
allowed_capabilities:
  - redis
  - swagger
  - embedded-ui
