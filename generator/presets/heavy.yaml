name: heavy
summary: production-track service baseline
description: Keeps a broader ops-oriented service baseline with metrics, scheduler jobs, Redis support, Swagger, embedded UI, and stronger runtime defaults.
implemented: true
base: service-base
packs:
  - preset-heavy
default_capabilities:
  - swagger
  - embedded-ui
allowed_capabilities:
  - swagger
  - embedded-ui
  - redis
