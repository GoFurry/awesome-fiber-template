name: light
summary: plain Go-style service baseline
description: Keeps a lightweight runnable HTTP service with common middleware, sqlite startup, and a smaller application surface than medium.
implemented: true
base: service-base
packs:
  - preset-light
default_capabilities: []
allowed_capabilities:
  - swagger
  - embedded-ui
